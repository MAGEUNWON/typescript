{
  "include": ["src"], // 자바스크립트로 컴파일 하고 싶은 모든 디렉터리를 넣기
  "compilerOptions": {
    "outDir" : "build", // 파일이 생성될 디렉터리를 지정함. build 라는 파일에 만들어질 코드를 넣을 것이라고 알려줌
    "target": "ES6",
    "lib" : ["ES6", "DOM"], // lib을 사용해서 타입스크립에게 무슨 API를 쓸거고 어떤 환경에서 코드를 실행하는지 알려줄 수 있음.
    "strict" : true,
    "esModuleInterop": true,
    "module": "CommonJS"
  }
}
// ES3에는 const가 존재하지 않음. 화살표 함수도 없음. 
// target은 어떤 버전의 자바스크립트로 타입스트립트를 컴파일 하고 싶을지를 설정하는 것

// lib : 타입스크립트에게 어떤 API를 사용하고 어떤 환경에서 코드를 실행하는 지를 지정할 수 있음. 
// 프로그램이 브라우저에서 실행되면 LIB에 "DOM" 유형 정의를 할 수 있음. 
// DOM은 window, document 등임.

// 자바스크립의 어떤 버전이 어떤 환경에서 사용될지 정하는 것. 
// ES6를 지원하는 서버와, DOM 즉 브라우저 환경에서 코드를 실행시킬 거다 라고 말하는 것.
// 이렇게 설정하면 타입스크립트가 자동완성 기능을 제공해줌. 타입스크립트는 브라우저 API와 타입들을 알고 있기 때문
// 그냥 lib 안에 Es6만 쓰면 node.js로만 사용될 것이라고 생각함. 그래서 document. 했을 때 자동완성이 안됌


// 자바스크립트 파일과 모듈의 타입 정의를 작성하는 방법
